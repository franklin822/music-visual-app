info:
  name: Add Song
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3001/api/songs
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
